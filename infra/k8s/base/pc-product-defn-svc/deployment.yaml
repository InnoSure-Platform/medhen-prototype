apiVersion: apps/v1
kind: Deployment
metadata:
  name: pc-product-defn-svc
  labels:
    app: pc-product-defn-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pc-product-defn-svc
  template:
    metadata:
      labels:
        app: pc-product-defn-svc
    spec:
      containers:
        - name: product-defn-svc
          image: medhen/pc-product-defn-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http-rest
            - containerPort: 9090
              name: grpc-api
          env:
            - name: DATABASE_URL
              value: "postgres://postgres:postgres@postgres.medhen-core.svc.cluster.local:5432/product?sslmode=disable"
