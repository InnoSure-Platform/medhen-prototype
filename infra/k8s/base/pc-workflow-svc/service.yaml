apiVersion: v1
kind: Service
metadata:
  name: pc-workflow-svc
  labels:
    app: pc-workflow-svc
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 9090
      targetPort: 9090
      name: grpc
  selector:
    app: pc-workflow-svc
